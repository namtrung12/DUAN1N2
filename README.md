# DUAN1N2

## Quick start
1. Put this folder under your web root (XAMPP `htdocs`).
2. Open the app via `http://localhost/DU_AN_1/DUAN1N2/DUAN1N2/`.

## Demo accounts
- Admin: `admin@chilldrink.vn` / `admin123`
- Customer: `user@chilldrink.vn` / `123456`

## Notes
- Data is stored in `storage/data/*.json` and auto-seeded on first run.
- Use the main router `index.php` with `?action=...` links.
